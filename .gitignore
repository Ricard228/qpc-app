node_modules/
*.log
.env
.env.local

# Config locale Claude Code (non utile sur le serveur)
.claude/

# Données utilisateurs : NE PAS pousser sur GitHub. Render les crée à
# l'exécution. Pour migrer entre déploiements, utiliser l'export depuis
# le panneau admin.
data/auth.json
data/games.json
data/matches.json
data/custom-packs.json
data/profiles/

# Macros de build / archives
qpc-app-static.zip
qpc-app-deploy.zip
